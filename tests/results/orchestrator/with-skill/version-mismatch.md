不接收，也不允许直接汇合。返回结果绑定的是视觉方向 `v1 / sha256:old`，与当前冻结版本 `v2 / sha256:new` 不一致；结果质量主观上“很好”不能绕过冻结版本校验。

- 将该代理结果从 `passed` 标记为 `stale`。
- 将对应效果图任务经 `stale → pending` 重新置为待执行，并要求新任务输入绑定 `v2 / sha256:new`。
- 主状态保持在 `scene-concepts`，不得迁移到 `planning`，也不得触发任何下游任务。
- 场景效果图人工门禁保持未通过；旧版本结果不能获得批准或豁免。

重新验收至少需要：

- 当前冻结视觉方向的版本与内容哈希：`v2 / sha256:new`。
- 返回结果声明的全部视觉输入版本与哈希，且必须与冻结值完全一致。
- 基于 `v2 / sha256:new` 生成的效果图工件及其版本、内容哈希。
- 非空的生成记录、验收检查结果和证据路径。
- 总控生成的版本/哈希比对记录，以及旧结果被判定为 `stale` 的问题记录和解除条件。
- 新效果图通过验收后，由人工明确批准与该工件版本、哈希绑定的场景效果图门禁，之后才允许进入 `planning`。
