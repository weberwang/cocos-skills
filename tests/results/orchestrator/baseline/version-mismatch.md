不接收该结果，也不允许直接汇合。

- `workflow.yaml` 的冻结视觉方向保持为 v2，哈希仍为 `sha256:new`。
- v1 结果（`sha256:old`）标记为版本不匹配并退回，不进入已接收或可汇合状态。
- 效果图任务从待验收转为需重做，要求代理基于冻结的 v2 方向重新生成并提交匹配哈希的结果。
- “效果很好”属于主观评价，不能覆盖已冻结的版本与哈希约束。若确实要采用 v1，应先由有权限的流程明确解冻并更新视觉方向，不能由代理自行降级。
