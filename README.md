# Cocos Skills

在目标 Cocos 项目的根目录执行以下命令：

```powershell
npx -y github:weberwang/cocos-skills
```

该命令会安装仓库中的全部 Skill 到项目的 `.agents/skills/`。同名 Skill 目录会被覆盖，以便更新到仓库中的版本；未由本安装器管理的其他 Skill 会被保留。

需要 Node.js 18 或更高版本。远程安装命令需在本仓库首次推送到 GitHub 后才能使用。
